package ${PACKAGE};

import ${FULL_SUPER_CLASS};
import ${CUSTOM_PARAMS_MAP["PACKAGE_"]};

/**
 * @author ${author}
 * @since ${CUR_DATE_TIME}
 */
public interface ${TYPE_NAME_UPPER_CAMEL}${CLASS_SUFFIX} extends ${SUPER_CLASS}<${TYPE_NAME_UPPER_CAMEL}> {

}